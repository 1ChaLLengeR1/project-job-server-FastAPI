server {

   listen 80;
   server_name praca.server.arturscibor.pl;

   location / {
        proxy_pass http://django_backend:7000;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
}
