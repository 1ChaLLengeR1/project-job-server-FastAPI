name: CI - Container Smoke

on:
  workflow_call:

jobs:
  container_smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run container smoke (build -> import -> boot -> /health)
        run: bash infra/scripts/ci/ci_smoke.sh
