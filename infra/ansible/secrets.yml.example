# Szablon sekretów infrastrukturalnych (ansible-vault).
#
# 1. Skopiuj ten plik jako secrets.yml i uzupełnij wartości.
# 2. Zaszyfruj: make vault_encrypt (hasło w env ANSIBLE_PASSWORD).
# 3. Commituj WYŁĄCZNIE zaszyfrowany secrets.yml.
#
# Sekrety aplikacji żyją w Dopplerze (projekt server_job, config prd) -
# tu są tylko sekrety infrastrukturalne potrzebne do deployu.

doppler_token: "dp.st.prd.CHANGE_ME"
git_pat: "github_pat_CHANGE_ME"
docker_hub_username: "arturscibor"
docker_hub_password: "CHANGE_ME"
