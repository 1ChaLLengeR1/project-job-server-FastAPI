services:
  backend:
    build:
      context: ../../..
      dockerfile: infra/dockerfiles/dockerfile/production.dockerfile
#    expose:
#      - "3000"
    ports:
      - "3000:3000"
    env_file:
      - ../../../env/prod.env
    volumes:
      - ../../..:/app
    # tryb dev (bind mount + reload + auto-migracje) - odkomentuj:
    # entrypoint: ["bash", "infra/scripts/docker_entrypoint.sh"]
    networks:
      - project_job

#  nginx_backend:
#    build:
#      context: ../../..
#      dockerfile: infra/dockerfiles/nginx/nginx.dockerfile
#    ports:
#      - "3000:80"
#    depends_on:
#      - backend
#    volumes:
#      - ../nginx/nginx.conf:/etc/nginx/conf.d/default.conf
#    networks:
#      - project_job

networks:
  project_job:
    driver: bridge
